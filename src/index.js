// import React from 'react';
// import ReactDOM from 'react-dom/client';
// import App from './App';


// const root = ReactDOM.createRoot(document.getElementById('root'));
// root.render(
//   <React.StrictMode>
//     <App />
//   </React.StrictMode>
// );


// We don't use React direactly on our React Components, but the JSX is defined inside the React. In short if we using JSX in our file, that JSX demands for the React to be imported.
import Raect from 'react';
// Cuz ReactDOM has a method, called render(). Which gonna help us to render our JSX.
import ReactDOM from 'react-dom';
